import { BreakoutRoomClient } from "@/app/(app)/breakoutroom/ui/BreakoutRoomClient";

export const metadata = {
  title: "Breakout Room",
};

export default function BreakoutRoomPage() {
  return <BreakoutRoomClient />;
}
